//go:build tools

// tools.go ancora dependências usadas apenas em ferramentas auxiliares
// (arquivos com //go:build ignore). Isso garante que o go.mod as mantenha
// mesmo que não sejam importadas no build principal.
package pesquisa_ordenacao

// Sem dependências de ferramentas no momento
